import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/" className="text-3xl font-bold text-gray-800 flex-shrink-0">
      <div>Logo</div>
    </Link>
  );
};

export default Logo;
